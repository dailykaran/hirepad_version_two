{
  "name": "ai-hr-interviewer-mcp",
  "version": "1.0.0",
  "description": "AI-powered HR recruitment application with automated audio interviews",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd src/backend && node index.js",
    "client": "cd src/frontend && npm start",
    "dev": "concurrently \"cd src/backend && nodemon index.js\" \"cd src/frontend && npm start\"",
    "build": "cd src/frontend && npm run build",
    "install-all": "npm install && cd src/backend && npm install && cd ../frontend && npm install",
    "get-gmail-token": "node getOauthRefreshToken.js"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "googleapis": "^166.0.0"
  },
  "devDependencies": {
    "dotenv": "^17.2.3",
    "nodemon": "^3.0.2"
  }
}
